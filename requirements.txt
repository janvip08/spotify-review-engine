# Phase 1 — Ingestion pipeline dependencies

# Play Store scraping
google-play-scraper>=1.2.7

# Async HTTP (App Store RSS, Spotify Community, Trustpilot)
aiohttp>=3.9.0

# Cloudflare / AWS WAF bypass for Trustpilot scraping
cloudscraper>=1.2.71
playwright>=1.40.0      # headless browser fallback (run: playwright install chromium)

# HTML parsing (Spotify Community Forum, Trustpilot)
beautifulsoup4>=4.12.0
lxml>=5.0.0

# YouTube Data API v3
google-api-python-client>=2.100.0

# Date parsing
python-dateutil>=2.9.0

# Env variables from .env file
python-dotenv>=1.0.0

# Utilities
pydantic>=2.5.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.23.0

langdetect==1.0.9
groq>=1.4.0

# Phase 3 — Embedding & Indexing
sentence-transformers>=3.0.0
chromadb>=0.5.0
tiktoken>=0.7.0
nltk>=3.8.0

# Phase 4 — Retrieval & Ranking
rank-bm25>=0.2.2

# Phase 5 — RAG Chatbot & Theme Clustering
anthropic>=0.30.0

# API server
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
